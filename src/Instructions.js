import React, { Component } from 'react';


class Instructions extends Component {  
    render() {
        return (
        	<div className="instructions">
				Watch the pattern. When the pattern is finished, repeat the displayed pattern by clicking or tapping on the same tiles in the same order.
        	</div>
        )
    }
}

export default Instructions;

